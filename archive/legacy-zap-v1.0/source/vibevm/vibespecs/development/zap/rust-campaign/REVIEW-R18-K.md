# Root review of domain usage bindings

Accepted bounded K source/documentation unit, 2026-09-14. Root read the complete
454-type public guide and its50internal-declaration appendix, inspected the
437literal/67macro mapping and actual generator code, and reviewed scoped
compilation, lint and conformance receipts. All nine product crates now select
public-type gates without exemptions or baseline widening. CLI's empty surface
is a future-regression guard, not a test count.

Qualified names distinguish knowledge/economics feasibility and domain/core
maturity or delivery types. The schema generator still uses its unchanged
wire-literal argument for serde and a separate documents argument for metadata.
Knowledge IDs and viewer/Owner generators retain their actual constructor,
static query ID, route and body arguments. Macro declarations are separately
accounted for because the conformance frontend does not expand them.

Root independently compared the three extracted blocks after removing spec
attributes and formatting whitespace while preserving string literals. Cost,
Dreamer removal and viewer blocks matched1595,401and248 tokens respectively.
The cost block contains12 types, so the extraction denominator is32 (12+9+11),
not the earlier receipt's31. This correction does not change the complete504
mapping. All original public import paths remain reexported.

A separate whole-source comparison matched116existing domain files after only
the explicitly inert metadata/import and macro-document-argument changes were
removed. Remaining differences are the reviewed extraction parent files,
previously accepted admission/raw-page test corrections, a test-only reference
alias, and the separately active runtime Ready-index implementation. Those
runtime changes are not accepted through this documentation review.

Domain check, formatting, strict library lint, two affected external test-target
compiles and active scoped conformance passed. Domain has zero doctests; its
coverage here is documentary and is not described as executed examples. No
source exceeds600lines. Final coherent specmap, runtime regression, canonical
publication bytes and installed-artifact verification remain separate gates.
