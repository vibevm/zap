# Root review of test structure and composed economics

Root accepts the bounded R18-G structure and R18-G-ECONOMICS corrections on
2026-09-14. Their reports/checkpoints retain captures, exact counts, failures
and successful reruns. This acceptance does not close final package checks.

The app and domain test/support splits retain their test identities and
assertion bodies. Named fixture inputs replace positional argument lists;
same-module includes preserve borrowed fixture ownership. The app macro's
accidental string substitutions were detected against captured bytes and
restored before acceptance. All50 changed/new files are within600 lines and
strict affected-target lint passes without provisional dead-code or
argument-count allowances. Existing app journeys and selected domain scenarios
pass. Root reviewed the report, sample extracted bodies, helper boundaries,
whole-transaction guards and the distinct pre-split execution evidence.

Five economics failures reproduced against the exact captured unsplit source,
so the repair is independently identified. Root inspected the final business
basis control: before mutation the configured provider's exact digest equals
the assessment, after Work generation/revision change it differs, and actual
adjudication refuses StaleBasis without advancing state. The fixture does not
merely change its expected error to conceal a malformed comparison.

Review-sidecar setup now supplies the exact captured active Charter/Intent/
Outcome context. Forecast setup uses its registered exact basis, rebinds the
assessment before capturing the digest, and supplies the required active
policy. Original outcome and ordering assertions remain. The complete current
economics_service target passes12/12 and strict lint.

The automatic-prefix and Owner-decision scenarios exposed an actual production
hook descriptor defect. The admission hook allowed changes to Hold records but
omitted their viewer-search contributions. V2 scopes now derive index families
from the same exact allowed record families, so permission to mutate records
is unchanged while every contributed index is declared. Root inspected that
diff and the registered contribution path.

Root found the analogous empty scope in the schema-1 provider: historical
replay passes that descriptor into prepare_index_rows against current registered
contributions. The same three-family mapping is now applied there. Historical
payload, observation, authority and epoch bytes are unchanged. This last
descriptor correction is supported by source-path review and the established
per-record index mechanism; a descriptor-equality unit is not counted as an
executed historical replay journey.

The raw RecordCompleteness adaptations are recorded separately under R17.
Final current-source regression/conformance, public-use coverage, installed
artifact verification and publication remain open.
