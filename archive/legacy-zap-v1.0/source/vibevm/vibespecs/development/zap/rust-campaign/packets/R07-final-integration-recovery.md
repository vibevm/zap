# R07 final production integration after transport interruption

##subagent-quiet-clause

Coding worker gpt-5.6-sol/xhigh. Read R07-economics.md and its exact standing
files, ../REPAIR-R07-CORE-API.md, ../REPAIR-R07-CORE-API-AMENDMENT.md,
../AMENDMENT-R07-EFFECT-IDENTITY.md, ../REVIEW-R08-R09-AMENDMENT.md,
../REVIEW-R07.md, checkpoints/R07.json (relative to the parent directory),
and current owned source. Workspace/package/shared target are unchanged.
No full boot, private stewardship, Git, model/launcher/local inference, NEXT,
publication, full host suite or blanket mutations. Root accepts.

The prior native worker failed with a model-access verification stream error.
The Owner explicitly said continue. No cargo/zap process remained when root
recovered. Preserve source and receipts; no replay of external effects. Actual
current code extends beyond its checkpoint, so reconcile source before edits.

Latest evidence: core/store tests and clippy green; store6/6, economics
contracts5/5, economics service5/5 including ordered dependent effects and
unrelated revision churn. Actual no-seed first lowering is also green through
R08. These are bounded receipts, not full R07 completion.

Finish real production semantic effect contracts and shared pure kernels,
especially ReviewApplied including relowering sidecars. The root's final
approved seam (partially implemented in effects.rs) is:

- EffectSimulationContext privately holds optional immutable AffectedScopeView
  with require_affected_scope() returning an error when absent. No authority,
  credentials, provider handles or external effects enter the context.
- For each effect, strict decode and contract.scope precede evidence. Resolve
  that product cell's SAME registered PayloadAffectedScope extractor against
  the current branch overlay through fixed scope/job providers. Attach the
  validated view, call the same product kernel, then validate/overlay mutations
  and derive after-basis. No independently invented scope or empty job list.
- The scope callback must not require evidence before it is derived. Missing
  required adapters/providers refuse. Replay repeats the same derivation.
  Do not change committed event DTOs just to serialize derived context.
- Per-effect approval stays separate from changing suffix-bundle identity;
  concrete mutation comparison includes sidecar mutations in this transaction.

Complete changed-business-basis refusal and independent-work/holds service
cases, actual ReviewApplied simulation/apply equality, remaining production
semantic kernels and scoped clippy/format. Reuse valid tests, do not expand to
the full host panel. No stub effect contract or unregistered helper counts.

Own core/effects/preflight/admission/control and existing R07 economics files;
R08 worker owns packet-read selection, lowering, app/runtime packet consumption
and its review-cause consumer. Coordinate exports/ApplyReview and any shared
registry file. Read current first-lowering bootstrap amendment from the R07/R08
docs; do not revert Candidate initial strategy or the atomic root path.

Checkpoint coherent units at least every five active minutes with actual UTC,
parsed atomic JSON and previous-valid backup. Update REPORT-R07-FINAL.md and
checkpoints/R07.json with exact code/evidence/remaining concerns. Return only a
reviewable boundary; root then accepts or requests concrete repair.
