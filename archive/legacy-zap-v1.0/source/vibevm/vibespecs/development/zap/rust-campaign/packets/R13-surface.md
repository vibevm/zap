# R13: Rust CLI and machine backend over the real application

##subagent-quiet-clause

Middle Rust implementation, gpt-5.6-sol/high; root dispatches/accepts/Git/publishes.
No full boot, private stewardship/credentials, local Qwen, model/launcher calls,
nested agents, NEXT work, full host tests or blanket mutations. Use the same
workspace/package and exact named standing Rust/core/attribution files as the
saved R11-runtime.md packet; read that packet for those file paths and reuse
already-read rules.

Read ../PLAN.md, ../RUST-API.md, ../API-AMENDMENTS.md, ../REQUIREMENTS.json scoped
R13, permanent ZAP-RUNTIME.xml, ZAP-DATA-AND-VIEWER.xml and
ZAP-RUST-STORAGE.xml under P/vibevm/vibespecs/flows/zap, and the legacy public
ZAP-CLI.md/ZAP-BACKEND-API.md for behavior parity only. Inspect current public
core/store/domain/runtime exports and application composition. Root supplies
exact ownership; intended files are zap-api/**, zap-cli/**, assigned zap-app
composition plus tests, REPORT-R13.md and checkpoints/R13.json. No sibling
implementation imports: compose through typed core application ports.

Deliver a real Rust zap binary and versioned machine interface. No Python
invocation or simulated success backend. Expose declared capabilities, store
create/open, query/command/control/observation routes, native driver rendezvous,
runtime step/run/inspect and recovery through the actual service. Advertise
only implemented registered operations; named but missing behavior refuses
with stable structured codes and a meaningful repair surface.

One typed request/response family serves CLI and backend. Persisted commands
carry exact identity, revision, basis and idempotency; authority arrives through
configured protected control/driver channels, never caller actor labels. Read,
worker proposal, trusted observation, coordinator and Owner routes remain
separate. CLI help and machine capability schemas describe actual input/output.
Secrets must not enter command-line values, logs, DTO debug, errors or snapshots;
use protected input/configuration paths and the core SecretInput boundary.

Implement bounded node/detail/search/ancestors/dependents/frontier/why-blocked/
affected-subgraph/revision-diff/fork/packet/history queries. Results bind exact
store/base/revision/query epoch, scope and completeness/continuation. A partial
or unknown traversal is never an empty complete result. Source/detail access
must enforce configured roots, identity and credential exclusions; no arbitrary
filesystem read through a graph link. Large regions need level-of-detail and
aggregation where available, not full-graph serialization for every request.

HTTP or equivalent machine server provides consistent snapshot plus resumable
event tail, duplicate/gap detection, bounded streaming and cancellation. Opening
a view invokes no model or semantic mutation. Readers should not block the
coordinator or hold write transactions; slow consumers must receive explicit
resync/backpressure rather than lose events silently. Default network binding
is local; nonlocal access is explicit configuration with existing authority.

Expose the complete observable denominator, with availability honest per stage:
strategic/low-level lineage, maturity/debt, fog and relevance, source/evidence
applicability, untraveled forks, packet cost/omissions, roles/capabilities, native
intents/handles/receipts, liveness/retries, decisions/holds/economics, safe state,
completion blockers and recovery. Structured explanations come from stored
records, not model reconstruction. Future Qwik canvas/IDE consumes this service;
do not implement the UI or add a framework dependency.

Verify one real isolated application path through binary and backend: initialize
draft, inspect, authenticated control and command, current query, event stream,
restart/reopen, stale/idempotent request, read-route mutation refusal, foreign
cursor and guarded source access. Add targeted protocol negatives and streaming
recovery cases, not a duplicate domain suite. Use a neutral VibeVM fixture;
actual NEXT is never activated. No actual external model call in this task.

Checkpoint after coherent edits and before/after long commands, at most five
minutes while active, with structured serialization/parse/atomic replacement,
actual clock and previous valid copy. Use the shared Cargo target and coordinate
heavy checks. Deliver binary, public contracts and actual service-backed evidence;
report unavailable dependent operations and keep them on the campaign graph.
