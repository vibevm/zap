# Deterministic nonempty completion and candidate acceptance review

Accepted bounded integration evidence, 2026-09-14, under the Owner's
AMENDMENT-FINAL-VALIDATION.md. This closes the pending execution evidence for
the CandidateReview repair; it does not close the entire R16/R18 campaign.

The deterministic two-job scenario uses one nonempty campaign, actual captured
materials and sealed contracts, programmatically generated input, independent
producer/oracle arithmetic and exact output parsing. Both results pass the real
Rust runtime observation, candidate/provenance, evidence, stage and work
acceptance routes. The same complete ApplicationService agrees on readiness
and commits CampaignClosed. The reported focused receipt is 1/1, 1.96 seconds.

This exercises the formerly broken producer-Dispatch versus acceptance-basis
boundary with runtime-created candidates. The earlier source review, drift and
fresh-review recovery proof remain applicable. The runtime Checked stage is
checked before its existing domain Functional mapping; no algorithm or authority
rule is relaxed to close the fixture.

The driver is explicitly deterministic fixture infrastructure. No actual native
collaboration task, inference setting or slot release is inferred from it.
Two prior real native spawn refusals remain separate external-capability
evidence, and are not a release blocker under the Owner's updated validation.

Atomic native recovery has useful scoped receipts, but root review identified
two remaining corrections: late historical start after newer authorization,
and superseded waits after repeated distinct Unknown observations. Those are
kept open for bounded deterministic checks, together with remaining production
floor and final ledger/artifact work. No unchanged completion panel was rerun
solely for this review.
