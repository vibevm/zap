# R13B: usable graph navigation and typed details

##subagent-quiet-clause

Middle gpt-5.6-sol/high; start only on root dispatch after the bounded server
slice. Read R13-surface.md and reuse its exact standing rules and current API.
This slice implements its already authorized query denominator. No UI frontend,
model calls, local Qwen, NEXT execution, full host tests, Git or publication.

Own API/CLI/app wiring and, when explicitly assigned by root, a new
zap-domain/src/viewer_queries/** module plus its tests. Coordinate module and
registry additions with the existing domain integrator. Domain-aware query
logic lives with the domain; zap-api must not import sibling implementation
crates. Shared DTOs/ports follow the current core boundary and typed registry.

Deliver real registered node/detail/search, ancestors/children/dependents,
frontier/why-blocked, affected-subgraph, revision-diff and history queries.
Expose actual source/fact/region/evidence, work/obligation/contract and available
decision/hold/runtime detail. Preserve stable typed identities, parent and
dependency direction, source and evidence lineage, current revision and exact
availability. Missing R07 readiness/control providers return explicit unavailable
state; this cannot block implementing the independent navigation/detail paths.
Later lowering/fork/packet families remain named integration obligations until
their source records exist, not fabricated empty complete responses.

Each response carries store/base/revision/query identity and scope, with bounded
pagination or traversal frontier/completeness. Unknown/dropped/missing references
and a traversal limit are not an empty complete graph. Distinguish a stale query
cursor from a corrupt store. Search and level-of-detail summaries must state
their scope/counts; never serialize the entire campaign for a single node click.
Use current indexes where implemented; preserve explicit R17 optimization debt
instead of concealing full scans or inventing performance measurements.

Guard any source/content endpoint against configured roots, path traversal,
credential files and source identity drift. A graph link is data and cannot
authorize arbitrary filesystem access. Stored explanations come from records
and checks; rendering a view must not invoke a model or mutate semantic state.
Public schema/help/capabilities enumerate only real registered query operations.

Exercise the compiled binary and HTTP query route over a deterministic mixed
fixture: hierarchical work, cross dependencies, sources, facts, regions and
evidence. Read details and both dependency directions, page and resume, compare
revisions and expose actual missing closure/unknowns. Include one foreign/stale
cursor and one guarded-content refusal. Reuse domain logic receipts instead of
duplicating every acceptance test; do not activate the actual NEXT campaign.

Checkpoint coherent source and actual run evidence within five minutes active
work, with structured JSON, real clock, parse-before-atomic-replacement and
previous-valid copy. Report the completed query list and exact remaining
protected-command/provider/fork/packet integration denominator. Root retains
full R13 acceptance and assigns the next bounded slice.
