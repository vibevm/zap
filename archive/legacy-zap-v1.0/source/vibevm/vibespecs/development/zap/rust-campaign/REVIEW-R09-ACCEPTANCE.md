# R09 protocol acceptance

Root accepts the bounded schema-2 offline/return/reassessment implementation
described in REPORT-R09-FINAL.md. This acceptance includes the integrated R08
executable-lowering and current packet/runtime claim boundary. It does not
claim a finished production archive adapter, public transport or release.

The real registered-service journey creates a job through NativeBridge, records
its actual candidate and provenance, imports a Ready bundle return, performs a
no-change review, counts a failed approach exactly once, and applies a later
causal review and same-ID relowering. Work/contract versions and validation
generation advance once; prior result and execution history survive cold replay.
Root reviewed the report and the changed applicability and sidecar consumers.

Two concrete root findings are resolved in source. Current return applicability
checks live Work, active job, validation generation, active contract and current
Dispatch basis in addition to captured strategy/lowering/packet identity. Review
sidecars preserve their pre-review candidate basis; their lowering consumer uses
the identical producer/Work/contract/generation/basis filter and compares exact
candidate IDs. A stale sibling remains historical; a new matching candidate is
CAS drift. Neither lowering basis nor packet render basis substitutes for the
actual post-dispatch execution basis.

Accepted receipts: packet_resolution_service 1/1, lowering_offline 1/1,
lowering_semantic_service 1/1, scoped app/domain clippy with warnings denied,
and owned-file Rustfmt. Root reuses those relevant receipts and does not rerun
the same tests. Deterministic host/archive fixtures are explicitly simulated.
The executed fixture has one packet per bundle; multi-packet physical closure
belongs to the real adapter/integration proof, not an invented completed test.

R13C still supplies the protected service/CLI and native driver surface. R15A
supplies actual portable source/workspace/archive preparation and verification.
R16 verifies assembled product behavior and the complete requirement denominator.
NEXT remains inactive; no package publication is claimed by this acceptance.
