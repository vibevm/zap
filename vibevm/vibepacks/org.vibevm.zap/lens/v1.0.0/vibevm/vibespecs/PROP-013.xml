<?xml version="1.0" encoding="UTF-8"?>
<spec xmlns="https://vibevm.org/spec/1">
  <title id="root">ZapMockAgent and deterministic ZapMockModel</title>
  <status stage="spec" state="plan" comment="Owner addition of 2026-09-16 while finishing four-provider control. Use a deterministic non-LLM agent to prototype and verify public API/refactoring compatibility before spending live model tokens."/>
  <p><model fact="true" status="spec/plan">ZapMockModel is a deterministic algorithm, not an LLM wrapper. An explicit seed, scenario and ordered inputs determine its state, effects and trace. Time and gates are advanced explicitly by the test driver; model decisions do not depend on wall-clock timing, network randomness or credentials. Snapshot and restore preserve the same logical conversation and scenario position. Scenarios are a small validated test format, not arbitrary executable code or a claim to understand natural-language programming requests.</model></p>
  <p><agent fact="true" status="spec/plan">ZapMockAgent adapts that model to the same public agent, lifecycle and communication contracts used by real Zap integrations. It uses actual authenticated local HTTP/MCP, durable queues, question/answer acknowledgement, work reports and human review where the scenario requires them. It does not reach into application storage to manufacture success. A standalone process and an interactive terminal mode allow process/terminal supervision to be tested without a provider or model account.</agent></p>
  <p><identity fact="true" status="spec/plan">The mock has its own explicit product and model identities and visible synthetic provenance. It is enabled deliberately in test/development profiles and is never selected as a silent substitute for a failed real provider. Mock success establishes Zap contract and orchestration behavior, not compatibility with a particular vendor CLI, authentication path or native protocol.</identity></p>
  <p><scenarios fact="true" status="spec/plan">Initial scenarios cover a known response, a structured human question followed by an idle period, a late answer and explicit acknowledgement, controllable busy work, pause and continuation, checkpoint/restart, a managed result awaiting separate review, and two isolated actors or projects. Controlled failure cases cover duplicate events, stale epochs, uncertain acceptance and loss of a connection. Add future scenarios by composing the same small model actions rather than copying new ad hoc fake hosts.</scenarios></p>
  <p><scenario-corpus fact="true" status="spec/plan">Owner addition of 2026-09-16: keep reusable simulation scenarios beside the behavioral tests they explain. A scenario has a stable identifier, explicit feature coverage, deterministic seed and input tape, observable expected events and results, and its public-contract runner. Shared discovery validates and runs the corpus without an LLM. Support selecting scenarios and repeating them with recorded seeds; report the failing scenario, seed and input position so it can be replayed exactly. Separate pure model, local protocol, real process, real Git and provider-wire evidence; never describe a transport fixture as an actual provider test. A coverage index records supported features with scenarios and explicit gaps. New or changed orchestration behavior includes its scenario in the same change. Existing functionality is migrated into the corpus without pretending that undiscovered or missing scenarios passed.</scenario-corpus></p>
  <p><simulation-effects fact="true" status="spec/plan">Model actions remain deterministic data. The simulation harness performs bounded external effects through the product's real public contracts, inside isolated owned directories and local services. Process and Git completion are observed explicitly; logical scenario progress never depends on arbitrary sleeps. Actual asynchronous IO may use bounded timeouts to report a failure. Repetition creates fresh state and closes owned resources. The runner rejects unknown actions and unsafe file targets, and never evaluates scenario text as code or an arbitrary shell command.</simulation-effects></p>
  <p><verification-order fact="true" status="spec/plan">Meaningful contract and product-flow checks run with ZapMockAgent first. Refactoring and interface mistakes must be detected before new live-model probes. After the mock gate passes, bounded inexpensive real-agent checks establish the provider-specific mechanisms that a mock cannot prove. Existing transport fixtures remain useful for native wire details; they are not relabeled as full product or live-provider evidence.</verification-order></p>
  <p><isolation fact="true" status="spec/plan">The mock requires no LLM API key, has no provider SDK or remote inference route, and does not inspect user credential stores. Its communication is restricted to the explicitly configured local Zap service. Test state stays in an isolated directory, owned processes and observers close naturally, and managed input obeys the same human-control lease, permission and project pause fences as production agents.</isolation></p>
</spec>
