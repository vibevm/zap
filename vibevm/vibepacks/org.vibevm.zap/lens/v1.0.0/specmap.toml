# specmap.toml — this project's traceability policy
# (PROP-014; generated by `typescript-ai-native init`, then owned by you).

# The spec:// namespace this project's units are minted under.
namespace = "org.vibevm.zap/lens"

scan_roots = ["src"]
spec_roots = ["vibevm/vibespecs"]
root_spec_docs = []
exempt = []
dispositioned = []

# No installed packages with spec trees were found under vibevm/vibedeps/.
# After `vibe install`, re-run `typescript-ai-native init --force` (or add
# [[external_specs]] entries by hand) so cross-package citations resolve.
