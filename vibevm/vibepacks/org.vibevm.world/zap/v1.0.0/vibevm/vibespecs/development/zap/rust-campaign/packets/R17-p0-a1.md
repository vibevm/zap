# R17 P0 and A1: measure and reduce byte-identical allocation cost

##subagent-quiet-clause

Coding worker gpt-5.6-sol/xhigh. Start only on root dispatch. Read
R17-representation-design.md and its exact standing/source paths,
../REPAIR-R17-REPRESENTATION.md, ../PERFORMANCE-OBSERVATIONS.md and the
current wire/store code and relevant tests. No full boot, private stewardship,
Git, external model/launcher/local inference, NEXT, publication, full host panel,
or blanket mutations. Root accepts. Exact shared Cargo target remains
C:/Users/olegc/.vibe/zap/build/next-rust.

Root authorizes ONLY the bounded P0 and A1 portions of that contract. A2 physical
schema migration and B artifact-backed logical import are not selected. Do not
touch the actual published R14 store or repeat its17-minute import/audit. Do
not run a final actual-sized comparison until root chooses the stopping boundary.

P0 attributes allocation/bytes/CPU by small deterministic fresh-process phases
before optimizing. Preserve phase receipts and distinguish debug/release,
process peak from allocator attribution, and disk allocation from file length.
Do not fabricate unavailable counters. Verify the fixture's exact counts and
byte/digest summary; a timing without equivalent inputs is not a comparison.

A1 owns only zap-wire/src/canonical.rs and a bounded internal helper module if
needed, the streaming zap-store/src/history.rs projection digest, exact scoped
tests and the probe. Coordinate lib exports with R07; do not edit its
admission/effects/preflight or R08 read ports/app/runtime. The same source may
have moved; read current code before patching.

Preserve the prior encoder as a nonproduction differential reference. New
canonical bytes, digests and errors must agree, including finite float
boundaries, f32 values whose f64 cast changes shortest spelling, nested byte
vectors, numeric/string map keys and ordering, invalid keys/non-finite values,
duplicate/canonical ingress and Unicode. Do not silently broaden accepted wire
input. Preserve old projection length framing/order exactly when streaming.
Recompute the small probe after A1 and judge the contract's measured gains.

Deliver REPORT-R17-P0-A1.md with source identities, commands, nonzero test
counts, phase measurements and the specific evidence for or against further
A2/B work. Conditional B still needs an exact artifact-to-normalized-projection
validation/replay contract before selection; trusted preparer labels alone do
not replace the current v1 reducer's independent source rederivation.

Checkpoint source/probe process IDs and durable output at least every five
active minutes, using actual UTC, parsed atomic JSON and previous-valid backup
in checkpoints/R17-P0-A1.json. No opaque ongoing process survives only in chat.
Stop at this bounded report for coordinator judgment; root continues the full
implementation campaign independently.
