# R08 repair: executable lowering and authoritative packet resolution

##subagent-quiet-clause

Middle implementation, gpt-5.6-sol/xhigh under the Owner's clarified setting.
Medium effort is reserved for test executors evaluating ZAP behavior; it does
not apply to workers developing ZAP code. This overrides older implementation
effort headers in referenced packets.
Start only on root dispatch after
REPAIR-R08-EXECUTION-API.md is accepted. This bounded packet supersedes the
implementation order in R08-lowering.md; it does not discard its requirements.

Workspace: C:/Users/olegc/git/v/vibevm-next. Package P:
vibevm/vibepacks/org.vibevm.world/zap/v1.0.0. Development D is this packet's
parent directory. Use the shared CARGO_TARGET_DIR
C:/Users/olegc/.vibe/zap/build/next-rust with its exact spelling.

Read this packet, R08-lowering.md (including its exact named standing sources;
reuse earlier reads), ../REVIEW-R08.md, ../REPAIR-R08-EXECUTION-API.md,
../REPAIR-R07-CORE-API.md for consumed seams, ../API-AMENDMENTS.md and the
R08/R12 requirements in ../REQUIREMENTS.json. Do not run the full boot or read
private stewardship, credentials, unrelated source or the Python prototype.

Owner authorized complete ZAP implementation and publication; root alone
accepts and runs Git. No external launchers, nested workers, local inference,
NEXT execution, full host suite or blanket mutation testing. Imported NEXT
records are inactive data. Senior design reports are the contract to implement;
worker implementation reports remain candidates until root review.

Deliver the accepted contract's units 1-9: actual atomic lowering into Work and
TaskContract records, current conservation/proof/debt checks, closure of the old
graph-only live route, stored packet derivation, sealed core packet resolution,
app composition, runtime claim consumption and typed candidate validation.
Do not implement bundle export, return import or reassessment in this slice;
their existing unaccepted candidate code must be retained for the next packet.

Start with isolated core packet types and domain lowering files. R07's active
owner controls core registration/admission/commit/preflight/event schema files.
Send exact additive signatures and coordinate who makes each shared edit before
touching it. R14 owns legacy source, projection and import wiring; preserve its
additive domain registrations. Root resolves semantic disputes. No sibling
implementation crate imports or local substitute authority types.

The design must become real registered behavior. A fake resolver, caller-built
job record, unconditional eligible view, seeded current graph, or DTO-only test
does not establish the full path. Build one coherent service/redb journey from
permitted proposal/control routes through lowering, packet, privileged work
dispatch, service claim and candidate ingress. Include second lowering,
foreign-origin refusal, relevant vs unrelated changes, missing required bytes,
wrong producer/contract/result, rollback, exact retry and cold replay. Use
immutable historical observations for replay; do not rediscover the present
machine and pretend it was historical evidence. Reuse still-applicable R11
receipts and only rerun impacted cases.

Save each coherent edit unit and atomic structured checkpoints/R08.json using
actual UTC and a previous-valid backup at least every five minutes while
working. Report source paths, actual commands/results, remaining integration
and next operation in REPORT-R08-EXECUTION.md. Parse checkpoint JSON before
atomic replacement. Continue the assigned slice to a reviewable end; if shared
code is in flight, finish independent source and name the exact blocked seam.
