# R15A: production material, workspace and portable archive adapters

##subagent-quiet-clause

Coding worker gpt-5.6-sol/xhigh. Start only on root dispatch. Workspace and
package are those in R15-package.md. Read that packet for its exact standing
Rust files and boundaries, but this atom owns only the adapters below. The
indirectly named attribution XML is absent; skip it. No worker Git is allowed;
root retains attribution and commit responsibility. No full boot, stewardship,
credentials, external runner, model call, local Qwen, NEXT work, publication,
whole host panel or blanket mutations. Use the verified run-cargo.ps1 wrapper.

Read ../REPAIR-R08-EXECUTION-API.md, ../REPAIR-R09-RETURN-API.md,
../REPORT-R08-EXECUTION.md and the completed R09 report when available;
permanent ZAP-AGENT-PROTOCOL.xml, ZAP-RUNTIME.xml and ZAP-RUST-STORAGE.xml;
core execution_views/packet.rs, ArtifactStore public API, app packet_resolution
and cross_domain public seams. Inspect their specific implementations only as
needed to implement a real consumer. Do not read the complete legacy prototype.

Implement production PacketMaterialProvider, PacketWorkspaceProvider and
BundleArtifactProvider behind an app-owned module such as material_adapters/.
Resolve registered source/rule/fork identities using protected configured
bindings, immutable artifacts and the actual declared VibeVM query adapter
where needed. A caller's path, digest or principal label cannot establish
source identity, scope or authority. Capture actual bytes outside the DB write
transaction, verify exact length/digest and bind store/base/request identity.
Replays verify historical immutable captures; never recapture today's source
as evidence for yesterday's event. Missing or mismatched material is explicit.

Prepare workspace manifests from exact approved roots and declared operations;
preserve instruction isolation, permission, check and output constraints. Do
not imply that a manifest provides an OS sandbox. File access stays within
configured roots, refuses path traversal and symlink/reparse escapes, and
excludes credential/configuration material. Return content identity rather than
machine-specific source paths where the portable contract requires it.

Build and verify a portable bundle archive from the exact R09 derived manifest
and its required artifacts. The archive must be independently reopenable and
rehashable without this checkout or Python. Bound lengths/counts, duplicate
entries and destinations; refuse ambiguous names and overwrite. Validate
required closure and preserve immutable artifact publication/witness behavior.
The chosen container format is an implementation decision, not new authority;
document its version and expose machine-readable preparation/verification.

Coordinate factory/configuration DTOs with R13C before editing shared exports.
R13C owns service/transport/CLI/composition, R10 owns Dreamer, and R09 owns
cross_domain semantic rules. Do not change those meanings to make an adapter
pass. Avoid live filesystem access inside a transaction: if an existing seam
requires it, report the exact call path and propose a staged immutable capture
API for root approval instead of silently weakening the rule.

Own new adapter modules, their narrow public exports by coordination, scoped
adapter integration tests, ../REPORT-R15A.md and checkpoints/R15A.json. R15B
later owns packaging/docs/skills and installed consumer proof. Prove one real
source -> packet capture -> archive -> neutral-directory reopen/verification
journey plus relevant stale/path/partial-publication negatives. Do not repeat
domain or runtime suites. Checkpoint parsed atomic JSON after coherent work and
within five active minutes, keeping previous valid state. Stop this atom only
after a reviewable report; root continues the campaign.
