# R15: portable installation and truthful public package

##subagent-quiet-clause

Middle Rust/package implementation, gpt-5.6-sol/xhigh; root dispatches, accepts
and publishes. No full boot, credentials, local Qwen, model/launcher calls,
nested agents, NEXT execution, full host tests or publication from this worker.
Same workspace/package and exact standing rules as R04-store.md; reuse reads.

Read ../PLAN.md, ../REQUIREMENTS.json scoped R15, current public Rust API and
../VIBE-ADAPTER-FINDINGS.md (verified installed query surface and schema drift),
../VIBE-BUILD-CAPABILITY.md (actual lifecycle/build help and no-provider mode),
capability output, permanent ZAP-RUNTIME/ZAP-RUST-STORAGE/ZAP-AGENT-PROTOCOL,
package vibe.toml/.vibeignore/README/boot and the three zap-* skill files;
host PROP-024-code-bearing-packages.xml and actual installed vibe command help
needed for binary declaration/build/install. Root fixes exact write ownership;
intended surface is package metadata/public docs/skills and assigned thin Rust
entrypoints, REPORT-R15.md and checkpoints/R15.json. No host product changes.

Make the package build and run in an unrelated ordinary VibeVM project using
only shipped source and declared dependencies. Declare the Rust zap binary
through an actually supported VibeVM build mechanism. Keep build output outside
source identity and do not commit binary blobs. No path to this VibeVM source
checkout, undeclared global tool, GitHub account or Python runtime may be
required. Offline operation must work with materialized declared dependencies;
document actual prerequisites honestly.

Replace Python production descriptions and command examples with the real Rust
surface. Skills become thin native-agent instructions invoking typed utilities,
including durable dispatch/driver receipts, current gate checks, packet/resume,
goal capability/manual fallback, Dreamer and explicit authority. Quiet worker
packets never read the entire generated boot. Do not claim the Rust binary can
directly call harness-only tools without the cooperating driver.

Retain Python only as historical/nonproduction reference excluded from normal
payload/execution. Do not retry previously policy-denied .pyc deletion or remove
an entire directory to bypass that denial; exclusion/source-only packaging is
available. Never delete the only legacy evidence or unique accepted decision.

Audit shippable content: no private plans/stores/credentials, runtime checkpoints,
machine captures, full personal logs, build output or development generator.
Owner input and pilot files may be historical project material, but personal
execution captures must not leak through research directories. Resolve or
remove public links to excluded material; permanent normative specs/public APIs
carry the necessary product contract. English authored documentation, UPL-1.0
and package version1.0.0 remain unchanged.

Verify a clean source-only payload in a neutral isolated consumer: materialize,
build, CLI capability/query and one authorized fixture operation/reopen without
Python or access to host source paths. Use the actual supported VibeVM route,
not a hand-copied pretend install. Keep actual publication for root after R18.
Report unsupported harness operations by capability and evidence instead of
claiming all adapters are live-tested. No Qwik/IDE implementation here.

Checkpoint each coherent edit/operation and at most five minutes while active
using validated atomic JSON and previous valid copy. Coordinate shared Cargo
target and manifests. Deliver actual installation receipts, audited payload
contents and public contracts with remaining limitations explicitly mapped.
