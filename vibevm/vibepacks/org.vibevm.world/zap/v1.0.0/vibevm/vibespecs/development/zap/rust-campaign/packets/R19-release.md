# R19: publish version1.0.0 and close the implementation commission

Coordinator/root-only release procedure. Publication was explicitly authorized
by the Owner; use the existing commission after R18 acceptance. This packet does
not authorize a main-branch merge, NEXT product execution, local inference,
history rewrite, force push, secret changes or usage-reset redemption.

Read PLAN.md, the complete REQUIREMENTS/evidence disposition, accepted
REPORT-R18.md and the live campaign/checkpoint. Verify actual branch, source
revision, working tree, package identity and payload digest. Preserve unrelated
or unaccepted source; commit accepted work atomically under the repository's
ordinary attribution and Conventional Commit rules. Do not add model attribution.

Recheck the installed publisher's actual help and registry configuration. The
intended identity is flow:org.vibevm.world/zap@1.0.0 in registry vibespecs;
the earlier planned remote was https://github.com/vibespecs/org.vibevm.world.zap.git.
Inspect the current remote/manifest rather than assuming the preflight from
the Python prototype is still current. Use the ordinary authenticated VibeVM
registry publication path, not an improvised token or source-repository push.
Do not print credentials or reuse a publication token for source Git.

Verified 2026-09-14: the command is `vibe registry publish`, not a top-level
`vibe publish`. The current host checkout binary's help supports SOURCE,
--registry, --path, --dry-run, --json, --invoked-by and --agent-mode. Its
--repo-url mode is a separate direct-Git publication route and does not load
the registry publish token; do not silently switch routes. Recheck capability
before release and use --agent-mode agent / --invoked-by codex. This help-only
inspection performed no publication, authentication or remote writes.

Preflight the exact accepted source-only payload and declared dependencies.
Confirm production Rust commands, resolved public links, no Python runtime,
no compiled blobs/private data/development checkpoints and version1.0.0.
Publication approval is already present; do the concrete authorized operation
once the actual gate passes. Save the publication intent and receipt. If the
result is uncertain, inspect the registry/revision before retrying; do not
invent success or overwrite a different payload silently.

Install the published registry artifact into a fresh ordinary VibeVM consumer,
verify its identity/digest and declared dependency resolution, and exercise
the actual installed Rust binary's bounded capability/query and isolated draft
path. Reuse already applicable R15/R18 build evidence when the shipped bytes
match. Never activate NEXT to demonstrate the release. Record source and
installed payload identities separately.

Promote any remaining unique temporary-campaign facts before final retirement;
preserve the migration/documentation change record, original Owner input and
compact final execution receipt. Update the local execution pointer and full
campaign status from evidence, not from a green partial frontier. Do not leave
required production work under a completed status.

Checkpoint before and after publication and installation, with actual time,
structured JSON, parse-before-atomic-replacement, prior valid copy, command and
receipt IDs and unknown-effect disposition. The final answer should name the
published version, concise verified capabilities, exact important limitations
and the durable result/recovery records. After actual completion, stop and wait
for the Owner's further algorithm improvements as requested.
