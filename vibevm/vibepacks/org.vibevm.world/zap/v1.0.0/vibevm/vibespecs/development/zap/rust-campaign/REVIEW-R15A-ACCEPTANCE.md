# R15A adapter acceptance

Root accepts REPORT-R15A.md and the bounded production adapter/archive surface,
including the final timeout and module-split repairs. R13C service closure and
R15B installed-package proof remain separately tracked.

Root reviewed material configuration, archive framing and closure validation,
typed body checks, historical-claim integration and the process deadline path.
The archive contains full packet/claim and contract/job bodies, result contract,
capability, permission and stop-rule data, plus immutable raw materials. Neutral
entry inspection reads those bodies from the archive alone. The rejected
digest-only representation is not a completed product capability.

Historical export uses the exact job insertion from indexed record history and
its checked committed event preflight. Current-source mutation does not replace
the captured bytes or force a new historical capture. Filesystem verification
and publication occur outside the writer; transaction callbacks compare the
prepared logical evidence. ArtifactStore verified reopen reuses the existing
publication and witness implementation.

Output limits are enforced while reading the configured Vibe process. The
deadline continues after stream EOF, including the tested child that closes
both streams but stays alive; timeout does not join retained pipe readers.
Archive reads enforce the budget on one opened file, encoding budgets include
framing/footer, and foreign publication destinations are retained. Workspace
manifests explicitly do not claim to implement an OS sandbox.

Accepted evidence: real integrated service/archive journey1/1 after the split,
adapter tests3/3, configured offline Vibe query1/1, process and actual Windows
junction refusal tests, ArtifactStore restart/corruption tests3/3, scoped
app/store clippy and Rustfmt. Source modules are split by responsibility. Root
reuses these relevant receipts without repeating the same panel.

The integrated semantic journey has one packet; the adapter fixture separately
proves two-packet overlapping material capture. Full native protocol execution,
installed consumer, declared large-graph scale and release remain R16/R15B/R17/
R18/R19. No NEXT activation or package publication is implied by acceptance.
