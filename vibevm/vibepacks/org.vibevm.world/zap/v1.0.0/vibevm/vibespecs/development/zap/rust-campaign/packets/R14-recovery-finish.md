# Finish R14 receipt and staging recovery

##subagent-quiet-clause

Coding worker gpt-5.6-sol/xhigh; start only on root dispatch. Read
R14-migration.md and its exact named standing rules (reuse prior reads),
../REPORT-R14-FINAL.md, ../REVIEW-R14-PROJECTION.md, relevant legacy-import
source/tests and accepted shared APIs. No full boot or private stewardship.
Workspace C:/Users/olegc/git/v/vibevm-next; package
vibevm/vibepacks/org.vibevm.world/zap/v1.0.0. Shared target
C:/Users/olegc/.vibe/zap/build/next-rust. Root accepts and runs Git.

Own only app/legacy_import/** and its tests plus narrowly coordinated CLI/import
registration fixes needed by current schema2. Preserve active R07 core/control
and R08 domain/packet ownership. No local inference, external launcher, NEXT
execution, publication, full host suite, blanket mutations or cleanup of the
policy-denied .vibe Subs zap directory.

The actual425/212/64/1292 inactive import already exists and has bounded
query/reopen and source-hash evidence. Never repeat its 17-minute audit or
overwrite it merely to verify these repairs. Use small honest fixtures.

1. Make receipt creation recoverable after interruption during the write.
   Prepare a parsed/validated temporary receipt and publish atomically with
   no-clobber semantics. Preserve any existing partial bytes; recognize and
   reconcile prepared versus final states against the exact source/command/DB.
2. Validate existing staging before use: a plain directory, no symlink/reparse
   escape, and no unrelated contents. Resume only the exact expected empty or
   precommit state, committed/no-receipt state, or matching completed receipt.
   Never turn an unrelated directory into a published import.
3. Use only the real official import cell/routes for the isolated importer.
   If the full campaign cell set now needs unrelated packet/runtime providers,
   compose the actual import subset with full record/query support rather than
   installing stubs or bypassing the reducer.
4. Against a coherent shared core, run meaningful app recovery tests for the
   above cases, official nonzero-history refusal, source drift, exact retry,
   reopen/audit; run the impacted legacy import-service audit after schema2
   integration. Verify a nonzero selected test count. Reuse unaffected corpus
   receipts. Scope clippy/format to the affected crates/files.

Save source units and structured atomic checkpoints/R14-RECOVERY.json with
actual UTC and previous-valid backup at least every five active minutes.
Deliver REPORT-R14-RECOVERY.md with exact source/evidence and any shared compile
blocker. Performance amplification remains the separately assigned R17 repair,
not an excuse to claim full R14 completion. No broad storage redesign here.
