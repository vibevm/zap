# R03: portable Rust foundation

##subagent-quiet-clause

Middle implementation, gpt-5.6-sol/high. Start only after root explicitly accepts
the R01 foundation contract. Owner authorized the full Rust MVP and frequent
filesystem checkpoints. Root reviews/accepts and handles Git/publication.
No full boot, nested agents, external model launchers, local Qwen, credentials,
central stewardship, NEXT execution, full host test panel or blanket mutations.

Workspace C:/Users/olegc/git/v/vibevm-next. P is
vibevm/vibepacks/org.vibevm.world/zap/v1.0.0.
Read this packet; ../PLAN.md; ../RUST-API.md; ../STORAGE-ADR.md;
../WORKER-BOUNDARIES.md; P/vibevm/vibespecs/flows/zap/ZAP-RUST-STORAGE.xml;
P/vibevm/vibespecs/flows/zap/ZAP-AGENT-PROTOCOL.xml; and these standing files:
vibevm/vibedeps/org.vibevm.ai-native.rust-ai-native-lang/1.0.0/vibevm/vibespecs/boot/20-stack-rust-ai-native-lang.xml;
vibevm/vibedeps/org.vibevm.ai-native.core-ai-native/1.0.0/vibevm/vibespecs/boot/10-flow-core-ai-native.xml;
vibevm/vibedeps/org.vibevm.ai-native.rust-ai-native-lang/1.0.0/vibevm/vibespecs/rust/GUIDE-AI-NATIVE-RUST.xml;
vibevm/vibedeps/org.vibevm.world.git-attribution-policy/1.0.0/vibevm/vibespecs/boot/55-flow-attribution-policy.xml;
vibevm/vibespecs/common/PROP-024-code-bearing-packages.xml.
For a triggered local operation, the packet permits only the Band-3 block of
these named Rust cards under that rust-ai-native-lang slot's vibevm/vibespecs/cards:
scaffold-b-typed-builders.xml, scaffold-c-runnable-contracts.xml,
scaffold-f-structured-diagnostics.xml, scaffold-g-doctests.xml,
scaffold-e-fast-loop.xml, scaffold-d-differential-oracle.xml.
Ask root for a missing packet rule rather than loading the boot lane.

Write only the R03 foundation files assigned by WORKER-BOUNDARIES.md plus
../REPORT-R03.md and ../checkpoints/R03.json. Package manifest changes required
to declare/install the real Rust discipline dependency are permitted only as
assigned there; do not modify host Cargo/config/dependencies or other tracks.
No homemade no-op specmark crate. Use the actual distributable toolchain seam.

Implement the accepted typed foundation and package-owned Cargo workspace,
portable dependencies, small cell modules, stable IDs/digests, structured
REQ-citing errors, pure validation, wire envelopes and public seams. Public
contracts have meaningful runnable examples. Imported arbitrary data cannot
become an untyped backdoor to core authority, roles or transitions.
Do not implement future APIs as success-returning stubs. Unsupported operations
must remain explicit and absent from implemented capabilities.

Save a checkpoint before editing, at each coherent edit boundary, before and
after long commands, and at least every five minutes while active. Record exact
files, candidate state, checks with results, process/session IDs, remaining work,
next action and the API revision. Save compilable files incrementally; root
must be able to recover if the model quota ends before the final report.

Use package-local/shared target directory specified by R01. Do not launch a
full host build; existing discipline tools are under
C:/Users/olegc/git/v/vibevm/target/debug/. Build/check only the foundation crate(s)
and run the necessary contract/codec tests once at a stable candidate. If using
a staged formal deferral, record it and its R18 discharge instead of claiming
production conformance early. Never repeatedly rerun a passing panel unchanged.

Signal the compilable public foundation to root as soon as ready so independent
domain and runtime workers can start. Later API change requires a documented
revision and coordination; do not silently edit a type consumed by another track.
Deliver source, scoped receipts and a short report with actual limitations.

Accepted revisions 8/9 are recorded in ../API-AMENDMENTS.md. R11 now owns
zap-core/src/agent/** and zap-core/src/execution_views/**; R03 keeps generic
core, authority, candidate provenance, manifests and lib.rs integration. R03
may update RUST-API.md for the recorded narrow changes and routine shared IDs,
not change campaign authority or required behavior. Read that amendment before
touching the shared runtime surface.
