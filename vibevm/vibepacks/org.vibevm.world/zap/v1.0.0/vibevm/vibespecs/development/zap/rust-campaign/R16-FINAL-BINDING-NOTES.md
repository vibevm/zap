# Root binding of retained evidence

These notes resolve the specific evidence-navigation gaps in
R16-REMAINING-EVIDENCE.md. They are not a publication receipt. The final
runtime correction, coherent source gate and installed artifact remain pending.

Root inspected the existing concrete tests and source contracts rather than
requesting another live-model campaign. The retained R08/R11/R12 receipts cover
these unchanged assertions:

| Guarantee | Exact retained evidence and source boundary |
| --- | --- |
| Responsibility routing, optional context and overflow | lowering_contracts::role_routing_and_packet_packing_preserve_authority_and_context checks algorithmic/Senior/Junior selection, a40-token required fragment retained under a64-token budget, optional unloading, Senior write refusal, missing mandatory material and mandatory overflow. |
| Unsafe abstraction and ordinary full boot | lowering_contracts::unsafe_abstraction_and_full_boot_for_ordinary_worker_are_refused checks full-boot exclusion and hidden authority refusal; the typed abstraction source retains reconstruction/invariant inputs. This is deterministic policy evidence, not a weak-model quality experiment. |
| Independent work and complete occupancy | runtime_contracts::independent_jobs_progress_and_conflicts_refuse and awaiting_job_does_not_consume_unrelated_subject_capacity exercise the pure selector with actual claims/capacity. The actual paginated coordinator and unfinished-effect cases are separately required from R17-RUNTIME-SCALE. |
| Paused/held readiness | runtime_contracts::paused_or_held_work_never_reaches_claim_selection distinguishes blockers before selection. Real R11/R16 and application-service routes supply separate persisted admission/launch/retry evidence. |
| Capability identity and operation-specific fallback | runtime_contracts::goal_operations_and_capability_cache_remain_honest tests unsupported Update, wrong-scope Create, identity invalidation, retained current evidence and pending contradiction. The real R12 store test separately commits projection/unsupported application and refreshed provenance. |
| Heartbeat versus useful checkpoint | runtime_contracts::heartbeat_and_checkpoint_are_different_observations checks coalesced unchanged heartbeat and a distinct useful checkpoint; execution, safe state and collection remain separate dimensions. |
| Malformed response preserves work | runtime_contracts::malformed_candidate_preserves_artifacts_and_limits_repair retains the exact artifact while moving from one same-basis repair to changed-input/profile waiting. |
| Service/epoch fencing | core trust::tests::service_seal_fences_old_handles_and_harnesses_may_host_multiple_principals checks a grant against the actual seal and a different seal and permits distinct principals on one harness. The R13C lease/store-guard review separately supplies process ownership; neither is a claim against a hostile administrator. |
| Every registered material record has an exact canonical view | REVIEW-R16-OBSERVABILITY.md binds the existing NoOp projected-record source path and direct/authenticated HTTP Charter proof. Specialized viewer/runtime/history routes remain their own bounded interfaces. Basis cost, key limits, absent-family semantics and transport bounds are explicit. |
| Canonical history, schemas and typed mutation | R04/R14/A1/A2 reviews bind checked ingress, official transaction permits, frozen codecs, exact retry and rederived projections. Current Rust type/registry and production-floor checks supplement those preserved behavioral receipts; a macro/type catalog alone is not a replay test. |
| Deployment and publication | R15B's earlier neutral consumer proves the mechanism only. A new final source manifest, exact offline install, isolated build and installed journeys must bind the current bytes before R19. |

Profile defaults are workflow/configuration policy, not a claim of actual model
resolution. Explicit desired/resolved values, capability observations and
authority channels remain separate. A single model may occupy different roles
without acquiring another role's authority. Successful native inference and
native goal application remain unobserved in the selected external probe;
the owner amendment removes their mandatory repetition. Deterministic protocol,
completion and recovery evidence is not relabeled as live-host success.

The accepted nonempty R16 route supersedes the older empty-outcome-only gap.
The later R08/R14 activation review supersedes the tiny-import activation gap.
Two produced-root comparison limitations remain explicit: base comparison
cannot claim arbitrary roots created only by earlier simulated effects. The
normalized legacy application import still requires revision zero, while the
frozen reader/archive preserves its separate supported history surface.

Root's final disposition must bind these notes, the43fact inventory, existing
bounded acceptance reviews, repaired runtime evidence and final artifact to the
complete279fact/24vision denominator. No task-overlap or documentation edge by
itself accepts a fact; no repeated large import or live model is required merely
to refresh an unchanged receipt.
