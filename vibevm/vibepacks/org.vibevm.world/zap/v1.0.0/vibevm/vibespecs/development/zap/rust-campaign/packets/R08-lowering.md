# R08/R09: strategic lowering, bounded packets and weak execution bundles

##subagent-quiet-clause

Middle Rust implementation, gpt-5.6-sol/high, starts only on root dispatch.
Owner authorized the full accepted vision; root accepts/Git/publishes. No full
boot, private stewardship/credentials, model/launcher calls, local Qwen, nested
agents, NEXT execution, full host suite or blanket mutations.

Workspace C:/Users/olegc/git/v/vibevm-next. P is
vibevm/vibepacks/org.vibevm.world/zap/v1.0.0.
Read this packet, ../PLAN.md, ../RUST-API.md, ../API-AMENDMENTS.md,
../REQUIREMENTS.json scoped R08/R09; the permanent ZAP-METHODOLOGY.xml,
ZAP-LOWERING-AND-DREAMER.xml, ZAP-AGENT-PROTOCOL.xml and
ZAP-ADAPTIVE-CYCLE.xml under P/vibevm/vibespecs/flows/zap;
standing sources: the host materialized rust-ai-native-lang 1.0.0
vibevm/vibespecs/boot/20-stack-rust-ai-native-lang.xml and
vibevm/vibespecs/rust/GUIDE-AI-NATIVE-RUST.xml; core-ai-native 1.0.0
vibevm/vibespecs/boot/10-flow-core-ai-native.xml; git-attribution-policy 1.0.0
vibevm/vibespecs/boot/55-flow-attribution-policy.xml, all under
vibevm/vibedeps/<qualified-package>/1.0.0 as in the previous packets.
Reuse earlier reads. Triggered Band-3 Rust cards: scaffold-a-generators.xml,
scaffold-b-typed-builders.xml, scaffold-c-runnable-contracts.xml,
scaffold-d-differential-oracle.xml, scaffold-e-fast-loop.xml,
scaffold-f-structured-diagnostics.xml, scaffold-g-doctests.xml,
scaffold-h-simulators.xml. Inspect only current owned domain/planning and public
core/wire/runtime seams, not the full package or Python implementation.

Root assigns exact file ownership before dispatch; intended surface is
zap-domain/src/lowering/** and shared domain seams, registered planning/bundle
cells and their tests. Shared DTO additions use core owned by the designated
integrator, no local substitutes or sibling imports. Reports REPORT-R08.md,
REPORT-R09.md and checkpoints/R08.json are owned here.

Implement the independent strategic graph, versioned lowering records and exact
many-to-many obligation lineage. Every active obligation, inherited stage debt,
deferral, verification and integration duty must retain an implementation or
authorized disposition. Coarse future work keeps explicit refinement triggers;
do not fabricate detail across an unknown architectural horizon. Prototype,
functional MVP and productization routes remain linked, not overwritten.

Semantic lowering changes meaning and uses actual adaptive/economics admission.
Pure rerender for another model/harness/context preserves the semantic contract,
authority, debt ledger and evidence applicability and must not trigger another
semantic approval. Material changed source/basis invalidates only affected
lowerings; global event churn alone cannot stale everything.

Implement role/capability routing from error consequence, novelty, context,
reversibility and verification cost. Senior cannot get production write scope;
Middle implements; Junior gets bounded cheaply verified work. All roles may
resolve to one weak model without creating more authority or stronger evidence.
Desired/resolved profiles and actual capability observations stay separate.

Implement real algorithmic packet assembly: exact instruction/source closure,
content-addressed dedup, fragments with inclusion reason/token estimate,
mandatory vs optional/unloaded/unavailable/unknown, bounded query handles,
prepared alternatives and result contract. Ordinary packets do not include the
full boot. Required data that does not fit causes split/relowering/retrieval or
an explicit capability wait; no silent truncation or universal ZAP token cap.
Capability-reported inherited context is shown honestly.

Material forks store premises, alternatives, recommendation, risks, costs,
conditions, selection authority, diagnostic action and stop boundary. A weak
executor can choose only an applicable delegated route; unknown conditions
require evidence. Untraveled alternatives are dormant, not obligations or jobs.
Abstraction carries an invariant-preserving concrete/abstract map and reverse
consumer checks; it cannot hide authority, migration or concurrency conditions.

Verification plans select affected units, consumers, meaningful negative cases
and current proof applicability. Reuse applicable receipts. Full-panel and
mutation recommendations require the specific accepted reasons, not a default
per leaf, stage or commit.

R09 exports an offline bundle with exact strategy/lowering/packet/source/
capability/permission/stop identities and referenced immutable artifacts. The
bundle is queryable without loading the full graph into a model. It contains
no credentials and performs no upload. Record weak attempts, choices, failures,
contradictions, observations, missing capabilities and unresolved questions in
an actual durable encounter journal.

Return import verifies all identities/digests and is idempotent. It preserves
newer strategy, classifies stale/conflicting evidence as review inputs and
never grants acceptance from an imported actor label. Keep prior history and
approach counters across cycles. Strong-model reassessment consumes a bounded
delta and emits new lowering only where needed. Entirely local execution is
supported inside the prepared envelope; no mandatory cloud/GitHub dependency.
Validate archive paths and artifact bounds; no traversal/symlink escape or
overwrite of an active store. Weak-mode simulations are labeled and never call
the installed local Qwen or claim its tested quality.

Prove one coherent strategy->lowering->packet->fork->encounter->return->revised
lowering cycle through registered service routes, plus conservation failures,
unrelated/related invalidation, missing context, stale/foreign bundles and
duplicate import. Use scoped tests and an algorithmically generated large
obligation fixture. Existing NEXT is data only and remains inactive.

Save each coherent source unit and structured atomic checkpoints with actual
clock/previous-valid copy at least every five minutes. Use the shared target
C:/Users/olegc/.vibe/zap/build/next-rust and coordinate heavy checks. Report actual
registered behavior and unresolved integration; no types-only completion claim.
