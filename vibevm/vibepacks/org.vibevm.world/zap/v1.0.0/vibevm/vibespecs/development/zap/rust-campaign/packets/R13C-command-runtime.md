# R13C: protected commands and actual runtime composition

##subagent-quiet-clause

Middle gpt-5.6-sol/high; root dispatches after a coherent read-server boundary.
Read R13-surface.md and reuse its named rules and current APIs; also read the
accepted ../REPAIR-R07-CORE-API.md and ../REPAIR-R08-EXECUTION-API.md for the
required composed providers and sealed data/packet contracts. Own API/CLI/app,
coordinate domain/runtime/core changes with their owners. No full boot, model
or local Qwen call, external launcher, NEXT execution, Git or publication.

Finish the original R13 mutation/control/runtime denominator through one actual
application service. FoundationComposition must retain the real
Arc<dyn ActionAdmissionProvider> produced by
zap_domain::economics::ChangeControlAdmissionProvider::new(), and inject it
through CommitServiceBuilder::action_admission_provider. Compose the actual
basis, capacity/readiness, affected-job and fixed completion providers.
Use the accepted R07 state-derived impact/effect-preflight/affected-scope
providers and the R08 ApplicationPacketResolutionProvider with immutable
historical replay captures. Required providers are fixed at service startup;
declaring a route or returning a capability label does not implement it. The
real runtime claim resolves a stored packet ID and operational IDs; do not
restore the retired caller-built job, producer or eligibility payload.
Missing required providers remain explicit unavailable state, never success
stubs. Both direct close and runtime completion use the same evaluator.

Expose exact typed command, Owner control, worker proposal, trusted observation,
native driver rendezvous, runtime step/run/inspect and recovery operations over
CLI/backend. Bind each channel to protected startup credentials/grants and the
exact campaign/store/driver principal. Caller actor/harness labels are data and
cannot create authority. If a necessary non-authorizing proposal grant factory
is missing, coordinate a lawful core surface instead of inventing authority.
Keep credential material out of argv, logs, schemas, errors and source queries.

One trusted active service owns each store lifecycle. Stop and drain its work
before reopening/replacing it; a new service seal does not revoke a still-live
old service. Persist/discover the endpoint and exact identity through a guarded
configuration boundary if CLI attaches to an existing daemon. Do not open a
second independent writer just because the caller changed mode.

Preserve command identity through timeout and retry. An absent row observed by
a concurrent status read is not proof an already-running submission cannot
commit later. Track/reconcile in-flight submission state or return an explicit
unknown observation; retry the same canonical command/id until its outcome is
known. Do not create a fresh effect identity solely because HTTP disconnected.
After confirmed no-commit/stale validation, rebuild only under the documented
protocol. No database transaction makes external native effects atomic; use
R11 intents, launch consumption, receipts and reconciliation.

Use one actual isolated binary/backend journey: create draft, protected Owner
activation, legitimate and forbidden-role command, current read/event tail,
duplicate/stale request, timeout/reconcile, pause/hold enforcement at both
service and runtime entry, cold restart and direct-close refusal/acceptance.
Native transport remains simulated in this deterministic fixture and the real
native probe stays R16. Do not execute actual NEXT or infer goal permission.

Checkpoint coherent edits and actual process/request identities within five
minutes active work, with real clock, structured JSON, parsed atomic replacement
and previous-valid copy. Report the complete capability surface and actual
source/evidence boundary. Root accepts the completed R13 denominator only after
query/server/control/runtime pieces compose and unresolved gaps are handled.
