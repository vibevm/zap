# R17 A2 compact physical storage

##subagent-quiet-clause

Coding worker gpt-5.6-sol/xhigh. Start ONLY on explicit root dispatch; this
saved packet alone does not start A2. Read R17-representation-design.md and its
exact named standing rules, ../REPAIR-R17-REPRESENTATION.md,
../REPORT-R17-P0-A1.md, ../REVIEW-R17-P0-A1.md and actual owned store source.
No full boot/private stewardship, Git, model/local inference, NEXT, publication,
full host panel or blanket mutations. Use the verified task-local run-cargo.ps1
wrapper with explicit package workdir.

The measured selection fact is that records plus two full history value copies
are75.07% of table values. A1 preserves bytes and reduces encoder/projection
peak; full commit/replay still has substantial amplification. A2's bounded
contract is the explicit physical schema2 atom, not a new storage engine or a
logical import redesign. B remains unselected.

Implement only the contract's checked binary current-record envelope, single
history body, secondary digest/reference and per-revision metadata, preserving
exact public logical values/history and all event/command bytes. Version the
physical catalog and snapshot/rebuild receipt shapes. Frozen v1 remains
readable/writable/replayable. No silent reinterpretation of old bytes or hashes.
Keep ordinary index rows unchanged and commit all required rows atomically.

Rebuild only into a fresh absent sibling through exact event replay, with
source/head/physical-schema-bound receipt, no-clobber publication, interruption
recovery and unchanged source/pointer. Never mutate or replay the actual NEXT
output merely as a convenient fixture. Test small controlled v1/v2 stores,
including corruption/truncation, both history orders, retry/reopen/audit and
unchanged logical-row digest. Preserve v1 projection digest semantics; different
physical digests must be named and versioned honestly.

Measure on the accepted SAME small fixture/profile. Required A2 gains and read
regression limits are in the contract; report failures, do not invent success
or automatically add compression/B. Do not call raw source-copy size encoded
attribution. No final actual-sized import is authorized until root selects the
final stopping boundary. A1 old/reference receipts remain comparison evidence.

Own zap-store physical/schema/engine/history modules and corresponding scoped
tests/probes. Coordinate shared exports with active owners; do not edit
R07/R09 semantic, app runtime or R14 recovery files to force a build. Every
coherent unit saves parsed atomic checkpoints/R17-A2.json with actual UTC,
previous-valid backup and process/partial-output identities within five active
minutes. Deliver REPORT-R17-A2.md and stop this subtask for root review. Root
continues the full implementation campaign and alone accepts/publishes.
