# R17: measured graph and history scaling

##subagent-quiet-clause

Middle gpt-5.6-sol/xhigh; root dispatches and accepts. Read R04-store.md and
R13-surface.md for exact workspace/package and standing-rule paths, reusing
prior reads. No full boot, local inference, external runner, NEXT execution,
Git/publication, full host suite or blanket mutation tests.

Read ../PLAN.md, ../STORAGE-ADR.md, ../RUST-API.md, ../API-AMENDMENTS.md,
R17 entries of ../REQUIREMENTS.json, V18/V21 in the accepted vision, permanent
ZAP-RUST-STORAGE.xml, accepted store/knowledge/surface reports and outstanding
index/performance debts. Own assigned benchmark harness and index/query changes;
coordinate shared source with its implementation owner before editing.

Current boundary: read ../REVIEW-R17-A2.md and ../REVIEW-R15A-ACCEPTANCE.md;
A1/A2 representation evidence is accepted and must not be rerun without a new
reason. B and actual425-node reimport remain unselected. R13C owns service/lease
repairs, R15B owns package metadata/vendor/consumer. Use the verified local
run-cargo.ps1 wrapper; no other Cargo target path. The missing package-local
attribution XML is omitted; no worker Git is permitted and root owns commits.

Measure the actual Rust service and public queries using deterministic mixed
work/knowledge graphs, both edge directions, evidence and a long committed
history. Record the machine, build profile, data generator/version, counts,
seed, source revision, warmup, query scopes, sample counts and percentiles.
Scale in steps toward the vision's hypothesis of100,000 nodes and1,000,000
edges; report an observed limit or resource bound honestly. These are benchmark
sizes, never product limits. Warm bounded query p95 below100ms is a hypothesis
to test, not a number to assert or hide behind a broad average.

Separate normal trusted-store reopen, explicit full replay audit, write latency,
bounded query latency, memory, disk growth, large-fanout invalidation, import and
index rebuild. Do not include initial dependency compilation in service timing.
Do not bypass official projection/index maintenance to manufacture a fast graph
unless that setup is explicitly a fixture and its distinct cost is reported.

Address measured bottlenecks with stable-identity and forward/reverse adjacency
indexes, source dependents, readiness/blocker counts, evidence applicability
and open-encounter indexes. Keep mutations and mandatory indexes in the same
transaction. Do not materialize all transitive closures or clone the entire
plan per Dreamer overlay. Large traversal/rebuild may be bounded and resumable,
with explicit progress/completeness, cancellation and stale-basis handling.

Demonstrate correctness on the optimized path using a small trusted reference
projection and changed-scope comparisons: unrelated updates preserve relevant
proof, selected dependency updates invalidate it, continuation never reports a
truncated world as complete, and stale indexes fail or rebuild explicitly.
Only add tests that distinguish an actual optimization risk. Reuse previously
valid correctness receipts and avoid rerunning scale fixtures after doc edits.

Check whether byte-identical source recapture unnecessarily invalidates reusable
verification: the current verification subject fingerprint normalizes Work but
may retain a Source record's revision/observation metadata. Distinguish changed
semantic inputs, scope and applicability from equivalent refreshed provenance.
Keep any resulting optimization exact and scoped; never drop a real dependency
merely to improve reuse measurements. R06's repair reviewer may refine this
observation before this task begins.

Keep generated stores outside the shipped package and shared host source.
Coordinate heavy benchmark runs; short scoped checks use Cargo's existing
cache lock. Checkpoint every coherent unit and before/after long runs using
actual time, structured JSON, parse-before-atomic-replacement and prior valid
copy; preserve live process IDs and partial measurement files. Deliver
REPORT-R17.md, reproducible harness and measurements with explicit limitations.
