# R10: isolated Dreamer planning and admitted scope changes

##subagent-quiet-clause

Middle Rust implementation, gpt-5.6-sol/high; start only on root dispatch.
Root accepts/commits/publishes. No full boot, private stewardship/credentials,
model/launcher calls, local Qwen, nested agents, NEXT work, full host tests or
blanket mutations. Same workspace/package/standing rules as R08-lowering.md;
read that packet's named standing files or reuse already-read copies.

Read ../PLAN.md, ../RUST-API.md, ../API-AMENDMENTS.md, ../REQUIREMENTS.json scoped
R10 and the permanent ZAP-LOWERING-AND-DREAMER.xml, ZAP-ADAPTIVE-CYCLE.xml,
ZAP-CHANGE-ECONOMICS.xml and ZAP-AGENT-PROTOCOL.xml. Root supplies exact disjoint
ownership on dispatch; intended surface is zap-domain/src/dreamer/**, its
registered cells/queries and tests, REPORT-R10.md and checkpoints/R10.json.

Implement detached planning overlays referencing exact base strategy/revision;
store a delta and its assumptions, questions, alternatives and estimates, not a
copy of the full live graph. Explicit add/remove requests and hypothetical
exploration are distinct typed commands. Ambiguous attachment remains a saved
question, not a guessed parent. Offer grill before new-goal admission and
persist answers/unresolved questions so context loss never restarts the whole
interview. Factual discovery and Owner preference questions remain distinct.

Simulation recalculates affected dependencies, knowledge/fog, value, cost,
obligations, proof and lowering. It performs no product dispatch, active
frontier mutation, approval consumption, authority change or live economics
hold. Incomplete inputs remain explicit. Algorithms execute projections; a
model may supply bounded semantic proposals through existing typed protocol.

Live application is an exact admitted semantic transition. Resolve relevant
concurrent drift, source/contract/basis, Owner/charter scope, four-hour economics,
stop state and live-job reconciliation using existing service gates. One exact
Owner response may bind charter amendment and economics for one prepared
envelope; do not re-ask identical authority. A stale dream never overwrites
unrelated live progress or silently discards applicable proof.

Removal/replace records every obligation, dependent work, artifact, evidence,
stage debt, deferral and external-effect disposition. It cannot remove a still
required prerequisite or manufacture original success by shrinking scope.
General Owner stop dominates all dream/apply/hold-release commands. Dormant
dreams do not block completion of unrelated active work.

Verify through actual registered routes: hypothetical changes leave live state
unchanged, grill resumes from saved state, attachment ambiguity, scoped drift
rebase, expensive promotion hold, exact authorization, removal with dependencies
and proof retention, and no side effects from simulation. Reuse current tests
for shared gates; do not create a duplicate full economics suite.

Checkpoint every coherent file unit and before/after long commands, at least
every five minutes, via structured serialization/parse/atomic replacement and
prior valid copy. Shared Cargo target and coordination follow R08. Deliver code,
machine views and scoped evidence, not an interactive UI or documentation-only
simulation. Root performs final acceptance.
