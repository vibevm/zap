# R15B: package metadata, thin skills and ordinary consumer proof

##subagent-quiet-clause

Coding/package worker gpt-5.6-sol/xhigh; begin only on root dispatch. Follow
R15-package.md and its named rules, with the missing attribution-file exception
and verified Cargo wrapper described in R15A-material-adapters.md. No full boot,
worker Git/publication, local Qwen, external launcher, NEXT or host test panel.

Temporary Owner instruction, 2026-09-14: use --offline for installations while
another worker fixes a possible online-install hang. Do not investigate or
repair that host bug here. This is an execution constraint, not a permanent
ZAP product requirement or evidence that online installation is unsupported.

Own package vibe.toml, .vibeignore, README/public usage and boot/skill content,
package-only metadata/dependency corrections by coordination, REPORT-R15B.md
and checkpoints/R15B.json. R13C owns CLI/API/service code and public exports;
R15A owns material_adapters and archive implementation. Request source fixes
from those owners rather than changing their live files. The accepted R13C
staged-capture amendment is the configuration and restart contract.

First verify binary declaration, package-relative dependency paths and the
source-only payload using VIBE-BUILD-CAPABILITY.md and actual installed command
help. Prepare a neutral ordinary VibeVM consumer through the real install/build
mechanism. Do not replace installation with a hand-copied package. A provisional
build can prove packaging while R13C/R15A finish, but the final consumer proof
must use their real protected service and concrete adapters and bind exact
shipped bytes. Reuse compiled dependencies and prior applicable receipts.

Public docs and thin native skills describe actual Rust commands, durable
packet/preparation/receipt/reconciliation behavior, capability/manual fallback,
Dreamer and economics. Do not assert a command works before its producer's
verified interface exists. Skills never pretend the binary invokes native
harness tools by itself. Mark host capabilities from actual evidence; do not
invoke unapproved local inference for discovery. Authored specs/docs stay English.

Exclude production Python, build products, personal stores/logs/handles,
development checkpoints and excluded research captures. Preserve historical
source and accepted decisions; do not retry denied cleanup. Verify every
shipped link has an included permanent target. Version remains1.0.0, UPL-1.0.
Expose actual prerequisites and capability-probe build support rather than
using version1.0.0 alone to infer lifecycle features. Root performs publication
only after R18. Checkpoint actual UTC and parsed atomic JSON with previous valid
state within five active minutes; report the final package/consumer identities.
