# R08 packet/runtime integration after transport interruption

##subagent-quiet-clause

Coding worker gpt-5.6-sol/xhigh. Read R08-executable-repair.md and its exact
named standing files, current REPAIR-R08-EXECUTION-API.md and overriding
REVIEW-R08-R09-AMENDMENT.md, checkpoints/R08.json in the parent directory and
actual current source. No full boot, private stewardship, Git, model calls,
external launchers, local inference, NEXT, publication or full host test panel.
Root accepts. Exact shared target is C:/Users/olegc/.vibe/zap/build/next-rust.

The previous native worker lost its stream while model access was temporarily
unverifiable; Owner explicitly said continue. Root verified no cargo/zap
process remained. Source has saved work beyond the checkpoint; reconcile it.

Actual baseline receipt: a real redb service with no seed or preexisting Work
parent passed charter/source/intent/outcome/strategy/lowering/packet routes.
Root+leaf+contract+coverage/current lowering committed atomically; a derived
packet was stored; related source recapture caused refusal without mutation.
Keep that meaningful test and complete the remaining actual service journey.

Finish app PacketResolutionProvider, runtime JobClaimPayload/cell/factory/
Coordinator consumption, actual producer/result/provenance checks, current
material/profile/workspace binding, ordinary applied-review relowering, exact
retry/reopen/audit, and source modularization. Do not implement R09 bundle/return
yet; its accepted contract supplies only needed forward shape/causation seams.

Root resolved the last missing selection API just before interruption:

    CurrentPacketSelection {
        store: StoreIdentity, observed_revision: Revision, work_id: WorkId,
        packet_id: PacketId, packet_digest: PacketDigest
    }

Add CampaignReadPort::current_packet(&WorkId, ReadAt) ->
Result<Option<CurrentPacketSelection>, ZapError>. This is a read-only core DTO/
port, implemented by app/domain from the unique current packet with exact
current lowering/strategy/origin and work/contract bindings at that snapshot.
None means not prepared/stale; conflicting current packets error. Coordinator
checks store/work and uses only returned PacketId plus generated operational
IDs. Normal CAS and sealed commit-time resolution rederive all meaning. Read
selection never grants authority. No change to WorkExecutionView and no default
successful/stub implementation. Missing packet is an explicit prepare/wait;
R13 may compose automatic pure preparation, never an invented packet ID.

You own this additive core read port/DTO, domain reader and runtime consumer;
coordinate shared exports with the R07 core owner. R07 owns effect simulation
evidence, core admission/preflight and production effect kernels. Preserve its
current changes. Ordinary review and return review share the accepted generic
review_cause sidecar and exact post-review CAS; lowering alone increments
generation once. Preserve old evidence and current-effect safeguards.

Checkpoint coherent units at most five active minutes, actual UTC and parsed
atomic JSON/previous-valid backup. Save REPORT-R08-EXECUTION.md and current
checkpoints/R08.json, exact source and scoped evidence. Tests need real
registered paths; do not seed packet/job/provenance or claim fake providers as
the whole product. Model-executed protocol probes later use Sol/medium; writing
ZAP/test infrastructure remains xhigh.
