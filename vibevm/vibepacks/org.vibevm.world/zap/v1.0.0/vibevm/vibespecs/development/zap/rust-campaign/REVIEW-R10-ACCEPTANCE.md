# R10 and held-job repair acceptance

Root accepts the registered Dreamer boundary in REPORT-R10.md, including the
combined Owner response and the shared R07 held-execution repair. This closes
the newly surfaced nonempty-job hold deadlock. Final product transport and
installed/native integration remain separate campaign work.

Root reviewed the report, both R10 amendments, the combined Owner cell and its
exact amendment validation, and the core held-job resolver and legacy digest
modes. The dedicated combined route uses existing charter and economics kernels
atomically. Only the saved action/mutable-obligation expansion is allowed; it
does not authorize unrelated charter edits or a generic batch. Ordinary
credentials do not imply the new control class. The later product retains
normal admission, pause and job checks and binds the exact active replacement.

The explicit combined preparation omits only the generic charter fingerprint,
replacing it with exact original/replacement bindings. Independent active
ChangePolicy checks remain. The executed scenario proves the same projection,
payload, item and local/comparison bases before and after the Owner event, with
no hidden recalculation write or second approval.

HeldExecutions records exact job/attempt/work/contract/generation identities and
checks their current safety even after the product moves them out of scope.
Current newly affected jobs are checked too. Missing, changed or unsafe state
refuses. ExactScope retains its prior request/view digest formulas and explicit
legacy defaults. The original nonempty-job removal/release scenario is proven.

Accepted receipts: Dreamer service4/4, economics contracts5/5, knowledge9/9,
ordinary Owner-decision regression1/1, scoped core/domain clippy and Rustfmt.
The four journeys cover detached/grill/ambiguity behavior, costly addition,
conserving removal and the combined decision, including pause, drift, retry and
cold audit cases. Root reuses the relevant receipts instead of rerunning them.

Additions use existing active outcome obligations; creating outcomes/obligations
remains the ordinary intent/outcome path. No produced-root comparison, full
frontend or external native execution is claimed here. R13C/R15/R16 must expose
and exercise these capabilities through the assembled installed product. The
Owner's strategic-map follow-up remains deferred until the main campaign ends.
