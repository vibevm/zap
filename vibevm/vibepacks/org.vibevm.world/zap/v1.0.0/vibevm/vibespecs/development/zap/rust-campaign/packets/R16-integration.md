# R16: integrated Rust MVP acceptance evidence

##subagent-quiet-clause

Middle gpt-5.6-sol/high; start only on root dispatch. Root accepts and performs
Git/publication. Use the workspace/package and exact standing-file paths named
in R13-surface.md; read that packet and reuse previously read rules. No full
boot, private stewardship, local Qwen, external launcher, NEXT execution,
blanket mutations or full host test panel. No production Python.

Read ../PLAN.md, ../RUST-API.md, ../API-AMENDMENTS.md, ../REQUIREMENTS.json,
the accepted R04/R06/R07/R08/R09/R10/R11/R12/R13/R14/R15 reports and remaining
review findings. Read the specific permanent requirement source before judging
its behavior. Own assigned integration fixtures and evidence records; request
the owning implementation worker to repair source defects. Do not replace
unavailable product paths with fixture-only success providers.

Build a requirement-to-evidence ledger for all279 normative facts and all24
vision sections. Preserve the denominator and stable IDs. Distinguish actual
runtime evidence, source-reviewed invariant, explicit environmental capability
limitation, and unfinished implementation. A stub or declared type is not a
runtime receipt. Reuse valid scoped receipts when their source boundary still
applies; do not repeat every unit test as an integration test.

Exercise a small set of coherent journeys through the actual application,
binary and registered providers in isolated fixtures:

1. Draft intent, charter and outcome; lower a nontrivial task through prototype,
   MVP and production obligations; produce a bounded typed worker packet,
   receive a candidate and retain independent acceptance/verification duties.
2. Capture real contained VibeVM query/source data, establish scoped evidence,
   recapture changed input and invalidate dependent proof. Revise the live plan
   with explicit uncertainty, value and work reconciliation. Unrelated proof
   remains reusable; no arbitrary IDs establish truth or remove fog.
3. Evaluate an inexpensive baseline and a costly beneficial change. Above-limit
   or unknown cost waits for Owner; a precise approval admits only the ordered
   effect envelope. Preserve the committed prefix across interruption, retain
   independent work, and keep a general Owner pause independent of approvals.
4. Run weak/external bundle export and import with exact lineage and stale or
   malformed result refusal. Explore a Dreamer overlay, grill it and adopt a
   reviewed proposal through the same live economics and authority gates.
5. Drive native rendezvous through claim, durable intent, current launch gate,
   trusted receipt, collection and safe state. Pause after enqueue and before
   driver pickup; prove no launch. Lose the mailbox and close/reopen the actual
   database; reconcile unknown effects before any retry. Record quota/overload
   waiting without creating a new architectural-failure attempt.
6. Compare completion readiness and direct campaign close on identical states:
   unfinished obligations, stale proof, active hold/pause, unknown effects and
   missing providers all refuse; only a fully satisfied fixture closes.
7. Query the same campaign through CLI/backend snapshot, detail and event tail;
   demonstrate restart, stale cursor, slow reader/backpressure and bounded
   completeness. Read requests must have no semantic or transport effects.

Use actual persisted Owner control, basis, eligibility, economics, capacity,
completion and affected-job providers. Inject only the external host boundary
where a deterministic transport simulation is needed. Label simulated transport
separately from a real native invocation. Root can conduct an explicitly scoped
native collaboration probe using a saved product request/receipt; never use an
external runner or local inference as a shortcut. Native goal operations that
require an explicit Owner/client action remain truthfully manual or unsupported;
do not manufacture that action or mark an unfinished goal complete.

The already isolated NEXT migration stays inactive and is validated by R14
receipts. Do not run its product tasks. Keep private fixture stores and host
handles outside the published package; use sanitized portable test fixtures.

Checkpoint every coherent unit and before/after long commands, targeting at
most five minutes during active work. Use actual clock, structured JSON,
parse-before-atomic-replacement and previous-valid backup. Produce REPORT-R16.md
and the complete evidence ledger, with concrete unresolved findings dispatched
to their owner and retained on the campaign graph. Root determines acceptance.
