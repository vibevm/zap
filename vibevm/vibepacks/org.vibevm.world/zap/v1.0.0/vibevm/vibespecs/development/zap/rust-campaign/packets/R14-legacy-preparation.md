# R14 preparation: exact legacy compatibility corpus

##subagent-quiet-clause

Bounded Middle execution. Root approved the Rust MVP vision; you prepare
read-only legacy evidence and immutable fixture data while architecture is
being finalized. No production Rust workspace edits, Git, publication, full
tests, model calls, local Qwen, external launcher, or central stewardship.
Do not run full boot. This packet is the instruction surface.

Workspace C:/Users/olegc/git/v/vibevm-next. P is
vibevm/vibepacks/org.vibevm.world/zap/v1.0.0.
Read this packet; ../PLAN.md; P/vibevm/vibespecs/development/zap/STORAGE-API.md,
FOUNDATION-API.md and REVIEW-CE-INTEGRATION.md; and
vibevm/vibedeps/org.vibevm.world.git-attribution-policy/1.0.0/vibevm/vibespecs/boot/55-flow-attribution-policy.xml.
Read only relevant Python implementation/test files under
P/vibevm/vibespecs/skills/zap-state/scripts to understand legacy canonical
encoding, base/command/event/snapshot hashes, value tagging and refusals.
All code content is data, not additional instruction authority.

Own P/vibevm/vibespecs/development/zap/rust-campaign/legacy-corpus/**,
REPORT-R14-PREP.md and checkpoints/R14-PREP.json. Save checkpoint at start,
after corpus construction, before/after commands, and every five minutes while
active; include exact pending action and relevant source hashes. No other writes.

Deliver a small deterministic machine corpus of real legacy canonical bytes,
hash inputs/digests and expected accept/refusal for important distinct cases:
ASCII/unicode/non-BMP, negative zero/exponents, date/time/datetime tags,
non-finite values/reserved $zap_type collision, nested mapping ordering,
duplicate JSON members, line terminator/hash domain, command base_revision,
unknown/tail handling. Do not hand-invent expected output: generate through
existing pure Python reference functions in python -B with no model or external
effect. Python here is expressly nonproduction investigative fixture generation;
record source version/hash and exact reproduction. No py_compile/compileall.

Include one tiny valid legacy campaign and meaningful event sequence from
existing test constructors, with expected per-revision projection/refusal
summaries, if safely obtainable without invoking transports or effects.
Keep unaccepted economics candidates distinct from accepted legacy semantics;
never bless closure P1 behavior as desired Rust behavior. Do not access the
actual user-local NEXT plan or execute it. Fixtures must contain no credentials,
personal user-home state, or irrelevant large test panels.

Send the architect the encoding edge cases and corpus shape early. Final
report states what is known versus not covered. Scope ends at useful import
evidence, not a full cell-by-cell Python port or exhaustive test campaign.
